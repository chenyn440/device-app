# TEST_PLAN

## Summary

这份文档整理当前项目的测试重点与验证路径，覆盖：

- 应用启动
- 调谐页
- 监测页
- 设置页
- 数据保存与加载
- GitHub 发布链路

当前项目仍以手工验证为主，尚未建立系统化自动化测试。

## 启动与基础验证

### 应用启动

验证项：

- 应用可以成功编译
- 主窗口可以打开
- 左侧导航可切换页面
- 顶部菜单和工具栏可见

### 默认状态

验证项：

- 默认进入调谐页
- 未连接时状态显示正确
- 页面不崩溃
- Mock 设备环境下基础控件可响应

## 调谐页验证

### 页面结构

验证项：

- 顶部操作按钮区可见
- 头部配置摘要区可见
- 主谱图与辅助谱图可见
- 右侧 `参数配置 / 仪器状态 / 设备连接` Tab 可切换

### 参数与联动

验证项：

- `Full Scan / SIM` 切换正常
- 检测器切换正常
- 稳定区切换正常
- 起始/终止质量数可编辑
- 调谐参数应用后不崩溃

### 状态与数据

验证项：

- 开始扫描后谱图刷新
- 仪器状态随设���状态更新
- 停止扫描后控件恢复可编辑
- 质量轴校正后有反馈

## 监测页验证

### 页面结构

验证项：

- 顶部方法按钮区可见
- `RIC / TIC` 图可见
- 右侧 `参数配置 / 仪器状态` Tab 可切换

### 参数区

验证项：

- 检测器切换正常
- 稳定区切换正常
- `Full / SIM` 切换正常
- `SIM` 表格可编辑
- 删除选中、设置按钮可点击

### 扫描流程

验证项：

- 运行方法后图表刷新
- 暂停刷新可切换
- 保存方法流程正常

## 设置页验证

### 连接配置

验证项：

- `本地连接 / 远程中转服务` 切换正常
- IP、端口可编辑
- 建立连接、断开连接按钮响应正常

### 存储配置

验证项：

- 文件路径可设置
- 文件名前缀可设置
- 自动保存开关可切换

### 管道服务测试

验证项：

- 发送文本可写入日志
- 清空数据可生效

## 数据页与持久化验证

### 单帧保存

验证项：

- 当前帧可保存为 JSON
- 当前帧可导出 CSV

### 单帧加载

验证项：

- 本地文件可重新打开
- 谱图与参数可恢复

### 方法保存

验证项：

- 监测方法保存成功
- 方法列表可枚举
- 方法可重新加载

## 异常路径验证

验证项：

- 未连接时禁止扫描
- 真空条件不足时启动被拒绝
- 文件损坏时不崩溃
- 设备异常时能提示错误

## 发布验证

### 本地打包

验证项：

- `scripts/package-macos.sh` 能生成 macOS zip
- `scripts/package-windows.ps1` 参数结构正确

### GitHub Release

验证项：

- 打 `v*` tag 后 workflow 触发
- macOS job 成功上传产物
- Windows job 成功上传产物
- GitHub Release 自动生成

## 当前测试现状

已验证：

- 本地应用构建与运行
- 大量 UI 交互手工回归
- macOS 打包脚本可生成 zip

待补：

- 自动化单元测试
- 自动化 UI 测试
- Windows 平台实机打包验证
- GitHub Release 端到端验证
